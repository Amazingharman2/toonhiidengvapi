{
  "name": "toonstream-scraper",
  "version": "4.0.0",
  "description": "Vercel edge scraper for toonstream — got-scraping edition (no CF proxy)",
  "main": "api/index.js",
  "type": "module",
  "scripts": {
    "dev":    "vercel dev",
    "deploy": "vercel"
  },
  "engines": {
    "node": ">=18.x"
  },
  "dependencies": {
    "got-scraping": "^4.0.4"
  }
}
